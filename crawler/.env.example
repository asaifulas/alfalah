# Vertex AI Configuration
# Required if you want to upload to Vertex AI
VERTEX_PROJECT_ID=your-project-id
VERTEX_LOCATION=us-central1
VERTEX_INDEX_ID=your-index-id
VERTEX_INDEX_ENDPOINT=your-endpoint-url

# Google Cloud Credentials
# Path to your service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Crawler Settings
# Size of text chunks (default: 1000)
CHUNK_SIZE=1000
# Overlap between chunks in characters (default: 200)
CHUNK_OVERLAP=200
# Maximum pages to process per PDF (default: 1000, 0 = no limit)
MAX_PAGES_PER_PDF=1000

# Request Settings
# Timeout for HTTP requests in seconds (default: 30)
REQUEST_TIMEOUT=30
# Delay between requests in seconds (default: 1.0)
REQUEST_DELAY=1.0
# User agent string for HTTP requests
USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"
